/* ═══ Direction B — THE GRADE (features.v3AtmoGrade) ═════════════════════════
   No new layer at all. The weather is expressed through things already on the
   glass — the three the owner widened tokens.css's monopoly to allow on
   2026-09-12:
     the mat    rain turns it cooler and a shade darker; dusk turns it warm
     the grade  rain desaturates and dims the card's photograph; dusk warms it
     the flash  a strike brightens the whole archive, mat and card together,
                as a room lit by lightning is lit all at once
   Everything is css/atmosphere.css keyed on `:root[data-atmo="grade"]`; this
   module only names the direction. The mat's change rides a CSS transition on
   the painted property, so a new reading arrives as a slow turn, not a cut.
   ═══════════════════════════════════════════════════════════════════════════ */

export const grade = { name: "grade" };
